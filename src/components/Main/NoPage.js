import "./NoPage.css";

const NoPage = () => {
  return (
    <div class="page404">
      <h1 class="heading">Uh oh, we can’t find that page...</h1>
      <p class="para">
        Sorry, the page you are looking for doesn't exist or has been moved.
      </p>
    </div>
  );
};

export default NoPage;
